<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN PAGE for {{companyName}} (views/adminedit.handlebars)</title>
</head>
  <ul id="users"></ul>
<body>
    
{{>header}}
<div class="admin-page-container">
        <h1>ADMIN PAGE for {{companyName}} (views/admin-crud-update.handlebars) testing update + delete button with alert window</h1>
        <h2>You can change the data of the user (id) here:</h2>
        <div class="admin-edit-container">
            {{!-- read user data --}}
                <p>Jane Doe email@mail.com +132</p>
                <p>Check-In Date: 15.03.2024 </p>
                <p>Check-Out Date: 19.03.2024</p>
                {{!-- update user data --}}
                <div class="new-date">
                    <p>New Check-In Date:</p>
                    <input class="check-in field" id="new-check-in-date" placeholder="New Check-In Date" type="new-check-in-date" value="" title="">
                </div>
                <div class="new-date">
                    <p>New Check-Out Date:</p>
                    <input class="check-out field" id="new-check-out-date" placeholder="New Check-Out Date" type="new-check-out-date" value="" title="">
                </div>
                <button onclick="confirmDelete()" id="delete-reservation" class="delete-button" type="submit">Delete Reservation</button>

                {{!-- the script for the alert window but it's not connected to the database --}}
                {{!-- this code can be changed --}}
                <script>
                    function confirmDelete() {
                    var x;
                    if (confirm("Are you sure you want to delete this reservation?") == true) {
                        x = "Reservation was deleted";
                    } else {
                        x = "Reservation was not deleted";
                    }
                    return x;
                    }
                </script>
        </div>
        <br>
        <button class="continue-button" type="submit">Save</button>
    </div>
{{>footer}}
</body>
</html>